@model IEnumerable<MijemApplication.Models.Contact>

@{
    ViewBag.Title = "Reservation List";
}

<div class="container tableContainer">
    <table class="table" id="contactTable">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.ContactName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.PhoneNumber)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.BirthDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ContactType1.TypeName)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.ContactName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.PhoneNumber)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.BirthDate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ContactType1.TypeName)
                    </td>
                    <td>
                        @Html.ActionLink("EDIT", "Edit", new {id = item.ContactID }, new { @class="btn btn-normal"})
                        @Html.ActionLink("DELETE", "Delete", new { id = item.ContactID }, new { @class = "btn btn-normal" })
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jquerydataTables")
    <script>
        $(document).ready(function () {
            $('#contactTable').DataTable({
                "columnDefs": [{
                    "targets": 4,
                    "orderable": false
                }], 
                bInfo: false,
                bLengthChange: false
            });
            $("#createButton").css("display", "inline");
        });
    </script>
}